const express = require("express");
const router = express.Router();
const User = require("../models/userModel");

// Rutas para usuarios

// TODO: Agregar rutas para registro, inicio de sesión, actualización, etc.

module.exports = router;
